export default function PatientTimeline({ patientId }: { patientId: string }) {
  // Group events by month
  // Show connected vertical line
  // Expandable event cards
  // Filter by event type
  // Show critical events in red
}